import { NameValidDirective } from './name-valid.directive';

describe('NameValidDirective', () => {
  it('should create an instance', () => {
    const directive = new NameValidDirective();
    expect(directive).toBeTruthy();
  });
});
